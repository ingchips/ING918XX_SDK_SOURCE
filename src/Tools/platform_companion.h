#ifndef _platform_companion_h
#define _platform_companion_h

/**
 * @brief Hotfix for platform.bin in ROM
 *
 * This function is provided by `Platform Companion` and must be called by app
 * in `app_main`.
 */
void platform_rom_hotfix(void);

#endif
